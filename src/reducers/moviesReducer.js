export default(state = [], action) => {
    switch(action.type) {
        case 'LOADING_MOVIES':
            return {
                
            }
        default:
            return state
    }
}